<%
/**
 * Vista: organizations/index.ejs
 * Lista de organizaciones (tenants) para Super Administrador.
 *
 * Variables locales esperadas:
 * - organizations: Array de organizaciones con _count
 * - search: String de búsqueda actual
 * - user: Usuario autenticado
 * - flashSucess, flashError: Mensajes flash opcionales
 */

/**
 * Formatea una fecha a formato colombiano dd/mm/aaaa.
 * @param {Date|string} date
 * @returns {string}
 */
function formatDate(date) {
  if (!date) return '—';
  return new Date(date).toLocaleDateString('es-CO', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric',
    timeZone: 'UTC',
  });
}

/**
 * Verifica si una fecha ya expiró.
 * @param {Date|string} date
 * @returns {boolean}
 */
function isExpired(date) {
  if (!date) return false;
  return new Date(date) < new Date();
}

/**
 * Obtiene las iniciales de un nombre para el avatar.
 * @param {string} name
 * @returns {string}
 */
function getInitials(name) {
  if (!name) return '??';
  const parts = name.trim().split(' ');
  if (parts.length >= 2) {
    return (parts[0][0] + parts[1][0]).toUpperCase();
  }
  return name.substring(0, 2).toUpperCase();
}

/**
 * Retorna clases y label para el badge del plan.
 * @param {string} planType
 * @returns {{ bg: string, text: string, label: string }}
 */
function planBadge(planType) {
  const map = {
    BASIC:      { bg: 'bg-gray-600', text: 'text-gray-100', label: 'Básico' },
    PRO:        { bg: 'bg-green-600', text: 'text-white', label: 'Pro' },
    ENTERPRISE: { bg: 'bg-yellow-500', text: 'text-brand-navy', label: 'Enterprise' },
  };
  return map[planType] || { bg: 'bg-gray-500', text: 'text-white', label: planType };
}
%>
<%- include('../../layouts/header') %>

<div class="p-4 sm:p-6 lg:p-8 space-y-6 bg-brand-navy min-h-screen">

  <!-- =========================================
       MENSAJES FLASH
  ========================================= -->
  <% if (typeof flashSucess !== 'undefined' && flashSucess) { %>
    <div class="rounded-lg bg-green-900/50 border border-green-600 p-4" role="alert">
      <div class="flex items-center gap-2">
        <span class="text-green-400 text-lg">✅</span>
        <p class="text-sm font-medium text-green-300"><%= flashSucess %></p>
      </div>
    </div>
  <% } %>

  <% if (typeof flashError !== 'undefined' && flashError) { %>
    <div class="rounded-lg bg-red-900/50 border border-red-600 p-4" role="alert">
      <div class="flex items-center gap-2">
        <span class="text-red-400 text-lg">❌</span>
        <p class="text-sm font-medium text-red-300"><%= flashError %></p>
      </div>
    </div>
  <% } %>

  <!-- =========================================
       ENCABEZADO Y BARRA DE ACCIÓN
  ========================================= -->
  <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
    <div>
      <h1 class="text-2xl font-display font-bold text-white">
        Gestión de Organizaciones
        <span class="text-brand-green">(Super Admin)</span>
      </h1>
      <p class="text-sm text-gray-400 mt-1 font-body">
        Administra los tenants y sus configuraciones de suscripción
      </p>
    </div>

    <div class="flex flex-col sm:flex-row gap-3">
      <!-- Búsqueda -->
      <form method="GET" action="/admin/organizations" class="flex gap-2">
        <input
          type="text"
          name="search"
          value="<%= search %>"
          placeholder="Buscar por nombre o NIT..."
          class="w-full sm:w-64 px-4 py-2.5 rounded-lg bg-brand-surface border border-brand-border
                 text-white text-sm placeholder-gray-500
                 focus:ring-2 focus:ring-brand-green focus:border-brand-green focus:outline-none
                 transition font-body"
        />
        <button type="submit"
                class="px-4 py-2.5 rounded-lg bg-brand-surface border border-brand-border
                       text-gray-300 text-sm font-semibold hover:bg-gray-700 transition">
          🔍
        </button>
        <% if (search) { %>
          <a href="/admin/organizations"
             class="px-4 py-2.5 rounded-lg border border-brand-border text-gray-400 text-sm
                    hover:bg-gray-700 transition flex items-center">
            ✕
          </a>
        <% } %>
      </form>

      <!-- Botón Nuevo -->
      <a href="/admin/organizations/new"
         class="inline-flex items-center justify-center gap-2 px-5 py-2.5 rounded-lg
                bg-brand-green text-white text-sm font-semibold shadow-md
                hover:bg-green-500 transition-colors">
        ➕ Registrar Organización
      </a>
    </div>
  </div>

  <!-- =========================================
       ESTADÍSTICAS RÁPIDAS
  ========================================= -->
  <div class="grid grid-cols-2 sm:grid-cols-4 gap-4">
    <%
      const totalOrgs = organizations.length;
      const activeOrgs = organizations.filter(o => o.isActive).length;
      const expiredOrgs = organizations.filter(o => isExpired(o.subscriptionEnds)).length;
      const proOrgs = organizations.filter(o => o.planType === 'PRO' || o.planType === 'ENTERPRISE').length;
    %>
    <div class="bg-brand-surface border border-brand-border rounded-xl p-4">
      <p class="text-xs text-gray-500 uppercase tracking-wide font-body">Total</p>
      <p class="text-2xl font-bold text-white font-display"><%= totalOrgs %></p>
    </div>
    <div class="bg-brand-surface border border-brand-border rounded-xl p-4">
      <p class="text-xs text-gray-500 uppercase tracking-wide font-body">Activas</p>
      <p class="text-2xl font-bold text-brand-green font-display"><%= activeOrgs %></p>
    </div>
    <div class="bg-brand-surface border border-brand-border rounded-xl p-4">
      <p class="text-xs text-gray-500 uppercase tracking-wide font-body">Expiradas</p>
      <p class="text-2xl font-bold text-red-400 font-display"><%= expiredOrgs %></p>
    </div>
    <div class="bg-brand-surface border border-brand-border rounded-xl p-4">
      <p class="text-xs text-gray-500 uppercase tracking-wide font-body">Pro/Enterprise</p>
      <p class="text-2xl font-bold text-brand-gold font-display"><%= proOrgs %></p>
    </div>
  </div>

  <!-- =========================================
       TABLA DE ORGANIZACIONES
  ========================================= -->
  <div class="bg-brand-surface border border-brand-border rounded-xl overflow-hidden shadow-lg">

    <% if (organizations.length === 0) { %>
      <!-- Estado vacío -->
      <div class="py-16 text-center">
        <span class="text-5xl block mb-4">🏢</span>
        <p class="text-base font-medium text-gray-400 font-body">
          <% if (search) { %>
            No se encontraron organizaciones para "<%= search %>"
          <% } else { %>
            No hay organizaciones registradas
          <% } %>
        </p>
        <a href="/admin/organizations/new"
           class="mt-4 inline-flex items-center gap-1 px-4 py-2 rounded-lg bg-brand-green text-white
                  text-sm font-semibold hover:bg-green-500 transition-colors">
          ➕ Registrar Primera Organización
        </a>
      </div>

    <% } else { %>

      <!-- Tabla responsiva -->
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-brand-border text-sm font-body">
          <thead class="bg-brand-navy/50">
            <tr>
              <th class="px-4 py-3 text-left text-xs font-semibold text-gray-400 uppercase tracking-wide">
                Logo / Nombre
              </th>
              <th class="px-4 py-3 text-left text-xs font-semibold text-gray-400 uppercase tracking-wide">
                NIT
              </th>
              <th class="px-4 py-3 text-center text-xs font-semibold text-gray-400 uppercase tracking-wide">
                Plan
              </th>
              <th class="px-4 py-3 text-center text-xs font-semibold text-gray-400 uppercase tracking-wide">
                Vencimiento
              </th>
              <th class="px-4 py-3 text-center text-xs font-semibold text-gray-400 uppercase tracking-wide">
                Config. Mora
              </th>
              <th class="px-4 py-3 text-center text-xs font-semibold text-gray-400 uppercase tracking-wide">
                Usuarios
              </th>
              <th class="px-4 py-3 text-center text-xs font-semibold text-gray-400 uppercase tracking-wide">
                Acciones
              </th>
            </tr>
          </thead>
          <tbody class="divide-y divide-brand-border">
            <% organizations.forEach(function(org) { %>
              <%
                const badge = planBadge(org.planType);
                const expired = isExpired(org.subscriptionEnds);
              %>
              <tr class="hover:bg-brand-navy/30 transition-colors">

                <!-- Logo / Nombre -->
                <td class="px-4 py-4">
                  <div class="flex items-center gap-3">
                    <% if (org.logoUrl) { %>
                      <img
                        src="<%= org.logoUrl %>"
                        alt="<%= org.name %>"
                        class="w-10 h-10 rounded-full object-cover border-2 border-brand-border"
                      />
                    <% } else { %>
                      <div class="w-10 h-10 rounded-full bg-brand-green/20 border-2 border-brand-green/40
                                  flex items-center justify-center">
                        <span class="text-sm font-bold text-brand-green">
                          <%= getInitials(org.name) %>
                        </span>
                      </div>
                    <% } %>
                    <div>
                      <p class="font-semibold text-white"><%= org.name %></p>
                      <% if (!org.isActive) { %>
                        <span class="inline-flex items-center px-2 py-0.5 rounded text-xs bg-red-900/50 text-red-400">
                          Inactiva
                        </span>
                      <% } %>
                    </div>
                  </div>
                </td>

                <!-- NIT -->
                <td class="px-4 py-4 text-gray-300">
                  <%= org.nit || 'N/A' %>
                </td>

                <!-- Plan -->
                <td class="px-4 py-4 text-center">
                  <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-semibold
                               <%= badge.bg %> <%= badge.text %>">
                    <%= badge.label %>
                  </span>
                </td>

                <!-- Vencimiento Suscripción -->
                <td class="px-4 py-4 text-center">
                  <% if (org.subscriptionEnds) { %>
                    <% if (expired) { %>
                      <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-semibold
                                   bg-red-900/50 text-red-400">
                        ⚠️ Expirada
                      </span>
                      <p class="text-xs text-gray-500 mt-1"><%= formatDate(org.subscriptionEnds) %></p>
                    <% } else { %>
                      <span class="text-gray-300"><%= formatDate(org.subscriptionEnds) %></span>
                    <% } %>
                  <% } else { %>
                    <span class="text-gray-500">Sin límite</span>
                  <% } %>
                </td>

                <!-- Configuración Mora -->
                <td class="px-4 py-4 text-center">
                  <div class="text-xs text-gray-400">
                    <span class="text-brand-green font-semibold"><%= org.moraGraceDays %></span> días gracia
                  </div>
                  <div class="text-xs text-gray-400">
                    <span class="text-brand-gold font-semibold"><%= org.moraMultiplier %>x</span> multiplicador
                  </div>
                </td>

                <!-- Usuarios -->
                <td class="px-4 py-4 text-center">
                  <span class="inline-flex items-center gap-1 text-gray-300">
                    👤 <%= org._count.users %>
                  </span>
                </td>

                <!-- Acciones -->
                <td class="px-4 py-4">
                  <div class="flex items-center justify-center gap-2">
                    <a href="/admin/organizations/<%= org.id %>"
                       title="Ver Detalles"
                       class="inline-flex items-center justify-center w-8 h-8 rounded-lg
                              bg-blue-900/30 text-blue-400 hover:bg-blue-900/50 transition-colors">
                      👁
                    </a>
                    <a href="/admin/organizations/<%= org.id %>/edit"
                       title="Editar"
                       class="inline-flex items-center justify-center w-8 h-8 rounded-lg
                              bg-yellow-900/30 text-yellow-400 hover:bg-yellow-900/50 transition-colors">
                      ✏️
                    </a>
                  </div>
                </td>

              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>

    <% } %>

  </div>

</div>

<%- include('../../layouts/footer') %>
