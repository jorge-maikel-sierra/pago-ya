<%- include('../layouts/header') %>

<div class="p-4 sm:p-6 lg:p-8 space-y-6">

  <!-- Header -->
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <h1 class="text-2xl font-bold text-brand-navy">Préstamos</h1>
      <p class="text-sm text-gray-500 mt-1">Gestión de préstamos de la organización</p>
    </div>
    <a href="/admin/loans/new"
      class="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg bg-brand-green text-white text-sm font-semibold shadow-sm hover:bg-green-500 transition-colors">
      ➕ Nuevo Préstamo
    </a>
  </div>

  <!-- Placeholder table -->
  <div class="bg-white rounded-xl border border-gray-200 shadow-sm overflow-hidden">
    <div class="p-8 text-center text-gray-400">
      <span class="text-4xl block mb-4">💳</span>
      <p class="text-lg font-medium text-gray-500">Lista de préstamos</p>
      <p class="text-sm mt-1">La tabla de préstamos se implementará próximamente.</p>
    </div>
  </div>

</div>

<%- include('../layouts/footer') %>
